# V4d veröffentlichen

In deinen bestehenden Repository-Ordner kopieren bzw. ersetzen:

- `index.html`
- `data/photo-masks.json`
- `data/photo-preview.jpg` nur falls dort noch nicht vorhanden
- `README.md` optional
- `GITHUB_UPDATE_V4D.md` optional

Bestehende Ordner wie `designs/` und `mask-calibration/` unverändert beibehalten.

GitHub Desktop:
1. Änderungen prüfen
2. Commit: `Integrate calibrated V4d photo preview`
3. **Commit to main**
4. **Push origin**
5. GitHub Pages mit `Cmd + Shift + R` hart neu laden
